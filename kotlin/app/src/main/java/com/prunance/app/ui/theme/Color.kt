package com.prunance.app.ui.theme

import androidx.compose.ui.graphics.Color

// ── Neutral palette (DeepSeek-style clean look) ──────────────────────
val White          = Color(0xFFFFFFFF)
val Grey50         = Color(0xFFFAFAFA)  // very light background
val Grey100        = Color(0xFFF5F5F5)  // card / surface
val Grey200        = Color(0xFFEEEEEE)  // dividers, outlines
val Grey400        = Color(0xFFBDBDBD)  // secondary text light mode
val Grey600        = Color(0xFF757575)  // secondary text dark mode
val Grey800        = Color(0xFF424242)  // primary text light mode
val Grey900        = Color(0xFF212121)  // headings light mode

val DarkBackground = Color(0xFF121212)  // dark-mode background
val DarkSurface    = Color(0xFF1E1E1E)  // dark-mode card / surface
val DarkDivider    = Color(0xFF2C2C2C)  // dark-mode dividers

// ── Brand accent (used sparingly: buttons, links, FABs) ──────────────
val BrandPrimary   = Color(0xFF1A1A2E)  // deep navy – professional, not flashy
val BrandSecondary = Color(0xFF10B981)  // emerald – positive actions / money
val BrandAccent    = Color(0xFF3B82F6)  // calm blue – links, highlights only

// ── Semantic colours ─────────────────────────────────────────────────
val Success        = Color(0xFF22C55E)
val Warning        = Color(0xFFF59E0B)
val Error          = Color(0xFFEF4444)
